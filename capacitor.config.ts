import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ResourcesRelationelles',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
